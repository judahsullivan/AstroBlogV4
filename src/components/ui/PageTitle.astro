---
import { cn } from '@/lib/utils';



interface Props{
    className?: string;

}

const {className } = Astro.props;

---



<h2 class={cn(
    `title relative py-5 overflow-hidden inline-block font-semibold w-full text-6xl md:text-9xl`
, className)}>
<slot />
</h2>